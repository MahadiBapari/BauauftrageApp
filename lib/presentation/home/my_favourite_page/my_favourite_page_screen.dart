import 'package:flutter/material.dart';

class MyFavouritePageScreen extends StatelessWidget {
  const MyFavouritePageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Favoriten-Seite',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}